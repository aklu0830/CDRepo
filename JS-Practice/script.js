function messager(element) {
    var swap = false;
    if (swap === false) {
        element.innerText = "Off";
        swap === true;
    } else if (swap === true) {
        element.innerText === "On";
        swap === false;
    }
}


