@using System.Net.Http
@using Microsoft.AspNetCore.Http
<h1>Welcome to the wedding planner, @ViewBag.LoggedInUser.</h1>
<button class="btn btn-danger">Log out</button>

<table class="table table-dark">
    <thead>
    <tr>
        <th>Wedding</th>
        <th>Date</th>
        <th>Guests</th>
        <th>Actions</th>
    </tr>
    </thead>
    <tbody>
    @{
        foreach (var VARIABLE in ViewBag.Weddings) {
            <tr>
                <td>@VARIABLE.WedderOne & @VARIABLE.WedderTwo</td>
                <td>@VARIABLE.Date.ToString("MMMM dd yyyy")</td>
                <td>@VARIABLE.Guests.Count</td>
                @{
                    if (ViewBag.LoggedInUser == VARIABLE.UserId) {
                        <td>
                            <a class="btn btn-danger">Delete</a>
                            <a class="btn btn-success">View</a>
                            
                        </td>
                    } else {
                        <td>
                            <a class="btn btn-success">View</a>
                            <a class="btn btn-primary">RSVP</a>
                        </td>
                        
                    }
                }
            </tr>
        }
    }

    </tbody>
</table>